const config = {
    // 启动端口
    port: 23238,
    // 数据库配置
    dataBase: {
        DATABASE: 'node',
        USERNAME: 'root',
        PASSWORD: 'password',
        PORT: 23437,
        HOST: 'localhost',
    }
};

export default config;
